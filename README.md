# ERP Integration & BI – One Command Project

Run everything with:

```powershell
python run_project.py
```

This installs deps, creates DB, parses EDIFACT, loads SQL Server, and opens BI connections.
